# Baghewala Digital Twin — Python dependencies
# Tested with Python 3.11+
#
# Install with:
#   pip install -r requirements.txt
#
# Note on torch: this pins a CPU/CUDA-generic install. If you have a GPU
# and want CUDA acceleration, install torch separately following
# https://pytorch.org/get-started/locally/ BEFORE running this file, or
# just leave this line as-is (a CPU-only torch install works fine for this
# prototype's small CNN).

numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
torch>=2.0
pillow>=10.0
matplotlib>=3.7

streamlit>=1.35

plotly>=5.18
